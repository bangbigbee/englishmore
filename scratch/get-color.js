const fs = require('fs');
// Very simple PNG parser just to get a general idea, or better yet, we can use an npm package!
// We are in a project with package.json, we can use `npx jimp` or similar, but maybe we can just install a temp package.
